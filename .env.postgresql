# DigitalOcean PostgreSQL Configuration
# Add these to your DigitalOcean App Platform environment variables
# IMPORTANT: Replace placeholders with your actual credentials. NEVER commit real credentials to the repository.
# SECURITY: Keep your database credentials secure and use environment variables in production.

PGHOST=your_database_host
PGPORT=25060
PGDATABASE=your_database_name
PGUSER=your_database_user
PGPASSWORD=your_database_password
PGSSL=true
DATABASE_URL=postgresql://your_database_user:your_database_password@your_database_host:25060/your_database_name?sslmode=require

# For local testing with PostgreSQL:
# source .env.postgresql
# npm run dev:full
